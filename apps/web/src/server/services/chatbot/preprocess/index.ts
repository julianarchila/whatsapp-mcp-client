import type { DomainMessage } from '../types';

// Placeholder for future steps (media fetch/store, audio transcription, etc.)
// For now, this keeps behavior unchanged.
export const applyPreprocessors = async (message: DomainMessage): Promise<DomainMessage> => {
  return message;
};


